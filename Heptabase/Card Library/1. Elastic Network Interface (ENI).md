### 1\. **Elastic Network Interface (ENI)**

- **Definition**: An ENI is a virtual network interface in AWS that connects a resource (e.g., EC2 instance) to a VPC subnet, allowing it to send and receive network traffic.

- **Connections**: The ENI is linked to a specific **subnet** within the **VPC** and gets an IP address from the subnet.