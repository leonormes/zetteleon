# The Editor Core:

This is the heart of Neovim, handling all of the fundamental editing operations such as inserting, deleting, and navigating text.

It also manages the buffer (the text you're editing), the window (the visual representation of the buffer), and the cursor.     

The core is written in C and provides a highly optimized and efficient foundation for the rest of Neovim.