ENI gets an IP from the subnet's range

The ENI is tied to a subnet, and when it's attached, it's allocated one or more IP addresses from the subnet's CIDR block (usually a private IP for internal communication).