# Spec Summary (Lite)

Implement a Go-based CLI tool to collect all user-assigned tasks and issues from Jira and Todoist. The tool will normalize this data into a uniform `WorkItem` structure and store it in a local SQLite database, creating a unified source of truth for all work.
