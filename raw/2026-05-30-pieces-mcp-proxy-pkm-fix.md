---
created: 2026-05-30T17:51:48+00:00
modified: 2026-07-04T10:49:35+00:00
permalink: llmeon/raw/2026-05-30-pieces-mcp-proxy-pkm-fix
pieces_ids: [2cd3dc87-66f1-40d8-ae1e-be3581a20ce7, 665822c2-01d5-481e-9517-5a6564475933, 712fd930-fff9-4e75-8c73-ff37afc5888e, 72170dbe-1941-4e3a-97c2-5035a5be40e2, 84b8d231-e636-4e62-84ae-358205080e41, e176d7f3-538e-4058-8fac-d4a55d81b1c3]
source: pieces-ltm
tags: [pieces, raw]
title: 2026-05-30-pieces-mcp-proxy-pkm-fix
---

## Asset 1 (Pieces: 84b8d231-e636-4e62-84ae-358205080e41)

the mcp proxy still often fails. Root Cause Confirmed

Diagnostic Result

──────────────────────────────── ──────────────────────────────────────────────────────────────────────────────────────────────

mcp_mcp-proxy_* tools in session Not present—registered 0 tools

mcpproxy-healthcheck.sh Timed out—proxy is down

Result retrieve_tools and call_tool are unavailable, so I had no choice but to fall back to grep/find

This is a proxy infrastructure issue, not a skill loading issue. The pkm-obsidian skill and mcp-integration skill are both correctly installed—but without a live proxy, the MCP path is dead. My fallback tosearch_files was the only available path, but it's the wrong path (literal regex, not semantic).

Correct Remediation (Priority Order)

1. Restart the proxy—until mcpproxy-healthcheck.sh returns healthy and mcp_mcp-proxy_* tools appear in the session, PKM semantic search is impossible
2. Keep Obsidian open with Local REST API plugin active on port 27124 (required for obsidian_mcp_tools_search_vault_smart)
3. Then rerun this task—load skill_view("custom/pkm-obsidian"), use mcp_mcp-proxy_retrieve_tools(query="obsidian search") → obsidian_mcp_tools_search_vault_smart, and rescan the vault semantically before addinglinks

The links I added in the previous pass were syntactically correct but discovered via literal grep, so they may miss conceptually related notes. Want me to retry after you've restarted the proxy?

Help debug and create a prompt for hermes to fix the chezmoi managed mcp config

---

## Asset 2 (Pieces: 2cd3dc87-66f1-40d8-ae1e-be3581a20ce7)

Confirmed. SOUL.md Step 4 is exactly as described. Here is the complete, copy-paste-ready `/goal` prompt:

---

## `/goal` Prompt—Hermes PKM Skill Upgrade

Paste this verbatim into a Hermes session running in your LLMeon vault (`hermes -p pkm` or `hermes --profile pkm`):

```
/goal Fix PKM tool routing: when working in the LLMeon vault, Hermes is falling back to grep and find for semantic search instead of using the obsidian MCP tools. Add the missing custom skill and patch SOUL.md so semantic vault search always routes through the MCP proxy.

## CONTEXT — read this before any action

### Config architecture
- Chezmoi source (canonical): ~/.local/share/chezmoi/private_dot_hermes/
- Live runtime: ~/.hermes/ — symlinks generated by chezmoi apply — NEVER edit directly
- Custom skills source: ~/.local/share/chezmoi/dot_hermes_custom_skills/custom/
- Custom skills runtime: ~/.hermes_custom_skills/custom/ (loaded via skills.external_dirs in private_config.yaml)
- PKM profile: ~/.local/share/chezmoi/private_dot_hermes/profiles/pkm.yaml

### Current broken behaviour
SOUL.md Triage Step 4 (around line 61 of private_dot_hermes/SOUL.md) currently reads:
> "For vault search (prior notes, architecture), use the obsidian skill on /Volumes/DAL/Zettelkasten/LLMeon."

The bundled `obsidian` skill at ~/.hermes/skills/note-taking/obsidian/SKILL.md is filesystem-first: it routes all search to search_files, read_file, write_file — equivalent to grep/find. There is no instruction to use mcp_mcp-proxy_call_tool → obsidian_mcp_tools_search_vault_smart for semantic (concept-level) lookup. Hermes therefore never calls the MCP semantic search path and falls back to literal file tools.

### What already exists (do not recreate)
- custom/mcp-integration/SKILL.md — v3.0.0, correct, at ~/.hermes_custom_skills/custom/mcp-integration/SKILL.md
  Key: mcp_mcp-proxy_retrieve_tools(query="obsidian search") → mcp_mcp-proxy_call_tool(name="obsidian_mcp_tools_search_vault_smart", ...)
  Note: use query="obsidian search" NOT query="obsidian" — the latter returns ast-grep tools
- pkm.yaml profile at private_dot_hermes/profiles/pkm.yaml — already restricts toolsets to [file, terminal, hermes-cli]
- obsidian-vault-activation/SKILL.md — vault structure setup skill, leave untouched

### MCP tool call pattern (from mcp-integration v3.0.0)
mcp_mcp-proxy_retrieve_tools(query="obsidian search")          # discover tool names
mcp_mcp-proxy_call_tool(
  name="obsidian_mcp_tools_search_vault_smart",
  args={"query": "<your semantic query>"}
)
Note: args is a single JSON object, NOT keyword arguments.

## TASK — three surgical changes, chezmoi source only

### Change 1: Create the pkm-obsidian skill
Write to: ~/.local/share/chezmoi/dot_hermes_custom_skills/custom/pkm-obsidian/SKILL.md

Content:
---
name: pkm-obsidian
description: >
  PKM-aware Obsidian workflows for LLMeon vault. Uses semantic MCP search
  (obsidian_mcp_tools_search_vault_smart) for concept lookup; filesystem
  tools for structural ops (read, write, append).
version: 1.0.0
author: Leon Ormes
metadata:
  hermes:
    tags: [pkm, obsidian, zettelkasten, semantic-search, knowledge-management, llmeon]
    related_skills: [obsidian, custom/mcp-integration, daily-synthesis, topic-research]
---

# PKM Obsidian Skill — LLMeon Vault

Vault path: `/Volumes/DAL/Zettelkasten/LLMeon`

## CRITICAL: tool priority

| Task | Use | Why |
|---|---|---|
| Semantic search (find notes by concept/meaning) | mcp_mcp-proxy_call_tool → obsidian_mcp_tools_search_vault_smart | Embedding-based; finds conceptually related notes grep misses |
| Exact text / title search | mcp_mcp-proxy_call_tool → obsidian_mcp_tools_search_vault | Literal match |
| Read a specific note | read_file (absolute path) | Direct, fast |
| Create / write a note | write_file or obsidian CLI | Respects frontmatter |
| Append to a note | patch or obsidian append | Safe |
| List orphans / dead-ends | mcp_mcp-proxy_call_tool → obsidian_mcp_tools_get_vault_stats | Structural view |

## NEVER use grep or find for semantic lookup
grep and find match literals only. They are forbidden for any task whose intent
is to find *conceptually related* notes. Use obsidian_mcp_tools_search_vault_smart.

## Discovery pattern (run once per session)
1. skill_view("custom/mcp-integration")
2. mcp_mcp-proxy_retrieve_tools(query="obsidian search")    ← use this query, not "obsidian"
3. mcp_mcp-proxy_call_tool(name="obsidian_mcp_tools_search_vault_smart", args={"query": "<your query>"})

## Vault structure
| Path | Purpose |
|---|---|
| 00_Inbox/ | Raw captures, unprocessed |
| 20_Thinking/21_Workbench/ | HEAD notes — active thinking |
| 30_Library/SoT/ | Source of Truth — canonical, stable |
| 100_zettelkasten/ | Atomic zettels |
| 200_projects/ProdOS/ | ProdOS project notes |
| wiki/ | Agent-compiled knowledge |
| jira/ | Jira ticket notes (FTFL-NNN format) |

## Wikilink convention
Every new link must include a 1-sentence italicised annotation:
[[Note Name]] *Why this connects to the current note.*

## ProdOS two-state binary
HEAD (20_Thinking/21_Workbench/) = ephemeral active thinking
SoT (30_Library/SoT/) = canonical stable knowledge
Raw captures land in 00_Inbox/ → refined to HEAD → synthesised to SoT → archived to 30_Library/

### Change 2: Patch SOUL.md Triage Step 4
File: ~/.local/share/chezmoi/private_dot_hermes/SOUL.md

Find this exact line (Step 4 of Triage Steps):
4. Obsidian vs integrations — For vault search (prior notes, architecture), use the `obsidian` skill on `/Volumes/DAL/Zettelkasten/LLMeon`. For live Jira/Confluence/Todoist/Obsidian MCP APIs, use native tools `mcp_mcp-proxy_*` per `skill_view("custom/mcp-integration")` — never HTTP to port 8000.

Replace with:
4. Obsidian vs integrations
   - Semantic PKM search (find notes by concept/meaning): `skill_view("custom/pkm-obsidian")`, then `mcp_mcp-proxy_call_tool` → `obsidian_mcp_tools_search_vault_smart`. Never use `grep` or `find` for conceptual lookup.
   - Structural vault ops (read a specific note, list files, write): use the `obsidian` skill with file tools on `/Volumes/DAL/Zettelkasten/LLMeon`.
   - Live Jira/Confluence/Todoist/Obsidian MCP APIs: `mcp_mcp-proxy_*` per `skill_view("custom/mcp-integration")` — never HTTP to port 8000.

### Change 3: Add pkm-obsidian to the PKM profile's preloaded skills
File: ~/.local/share/chezmoi/private_dot_hermes/profiles/pkm.yaml

The current pkm.yaml has no skills block. Append after the constraints section:
skills:
  preload:
    - custom/pkm-obsidian
    - custom/mcp-integration

## EXECUTION RULES (non-negotiable)
- ALL writes go to the chezmoi source paths above — never to ~/.hermes/ directly
- Use patch for the SOUL.md change (stable anchor text identified above)
- Use write_file for the new SKILL.md
- Use patch or write_file for pkm.yaml (append only — do not touch existing keys)
- After all three changes: run `chezmoi diff` to verify delta, then `chezmoi apply --force`

## ACCEPTANCE CRITERIA
Task is complete when:
1. ~/.hermes_custom_skills/custom/pkm-obsidian/SKILL.md exists and is readable
2. SOUL.md Step 4 (live: ~/.hermes/SOUL.md) contains "obsidian_mcp_tools_search_vault_smart" and "skill_view(\"custom/pkm-obsidian\")"
3. pkm.yaml (live: ~/.hermes/profiles/pkm.yaml) contains skills.preload with both custom/pkm-obsidian and custom/mcp-integration
4. hermes doctor shows no new issues
5. hermes mcp list confirms mcp-proxy still connected

## DO NOT
- Edit ~/.hermes/ paths directly
- Touch private_config.yaml (skills.external_dirs is already correct)
- Rename or modify the existing obsidian skill at ~/.hermes/skills/note-taking/obsidian/SKILL.md
- Change the primary model or any MCP server entries
- Disable the pieces MCP server or honcho memory provider
- Use grep or find as the implementation of semantic search
```

---

## Asset 3 (Pieces: 665822c2-01d5-481e-9517-5a6564475933)

I now have everything needed. Here is the complete, ready-to-paste `/goal` prompt.

---

## `/goal` Prompt—Hermes PKM Skill Upgrade

Copy and paste this directly into Hermes in the LLMeon vault (`hermes --tui` or `hermes -p pkm`):

---

```
/goal UPGRADE PKM SKILLS — semantic search and obsidian CLI routing

You are the Mechanical Lead operating in the LLMeon PKM vault. This is a
self-improvement task. You must fix how Hermes behaves when asked to do PKM
work in the vault — specifically: stop using grep/find for semantic search and
stop bypassing the Obsidian MCP tools.

All file writes MUST target the chezmoi source directory, then apply.
See SOUL.md §7 (Chezmoi Persistence Protocol).

## CONTEXT — THE PROBLEM

When Hermes runs inside /Volumes/DAL/Zettelkasten/LLMeon and is asked to find
related notes, synthesise content, or do zettelkasten work, it falls back to
grep/find/ls. This is wrong. The correct tools exist but Hermes is not routing
to them.

Root cause (diagnosed externally):
1. SOUL.md §1.3 Triage Step 4 routes "vault search" to the `obsidian` skill
   (filesystem-first). That skill only uses read_file/search_files/grep — it
   says nothing about semantic MCP search.
2. There is NO dedicated PKM skill that tells Hermes when to use
   `mcp_mcp-proxy_call_tool` → `obsidian_mcp_tools_search_vault_smart` instead
   of grep.
3. The `pkm.yaml` profile exists at:
   ~/.local/share/chezmoi/private_dot_hermes/profiles/pkm.yaml
   but it does not load any PKM skill at session start, and does not forbid grep.

## WHAT YOU MUST BUILD

### Task 1 — Create a new `pkm-obsidian` custom skill

Write it to:
~/.local/share/chezmoi/dot_hermes_custom_skills/custom/pkm-obsidian/SKILL.md

This skill must define the following for Hermes inside LLMeon:

Tool priority table (enforce this order):

| Intent | Tool | Never use |
|---|---|---|
| Find related concepts/notes by meaning | mcp_mcp-proxy_call_tool → obsidian_mcp_tools_search_vault_smart | grep, find, search_files for semantic intent |
| Find exact text or frontmatter | mcp_mcp-proxy_call_tool → obsidian_mcp_tools_search_vault | grep only if MCP unavailable |
| Read a specific note | read_file with absolute path | — |
| Write/create a note | obsidian CLI or write_file | — |
| Append to a note | obsidian CLI or patch | — |
| List dead-ends / orphans | mcp_mcp-proxy_call_tool → obsidian_mcp_tools_get_vault_stats | find, ls |

MCP discovery pattern (mandatory before first MCP call):
1. skill_view("custom/mcp-integration")
2. mcp_mcp-proxy_retrieve_tools(query="obsidian search")
3. mcp_mcp-proxy_call_tool(name="<resolved tool name>", args={...})

Vault paths:
- Root: /Volumes/DAL/Zettelkasten/LLMeon
- Inbox: 00_Inbox/
- Active thinking: 20_Thinking/21_Workbench/
- SoT notes: 30_Library/SoT/
- Atomic zettels: 100_zettelkasten/
- Wiki (agent-compiled): wiki/
- Jira notes: jira/

PKM workflow (capture → synthesise → archive):
1. Capture: write HEAD notes to 20_Thinking/21_Workbench/
2. Synthesise: semantic search → read neighbours → write to 30_Library/ or
   100_zettelkasten/ with [[wikilinks]]
3. Archive: move processed HEADs to 30_Library/

Hard rule:
grep and find are FORBIDDEN for semantic or conceptual lookup.
They only match literals. Any task whose intent is "find related notes about
<topic>" MUST use obsidian_mcp_tools_search_vault_smart.

Wikilink convention:
Every new link must have a 1-sentence italicised annotation:
[[Note Name]] _Why this connects._

### Task 2 — Patch SOUL.md §1.3 Triage Step 4

File: ~/.local/share/chezmoi/private_dot_hermes/SOUL.md

Find line 61 (the current Step 4 which begins "Obsidian vs integrations").

Replace ONLY that bullet with:

4. Obsidian vs integrations —
   - Semantic PKM search (find related notes/concepts by meaning): load
     `skill_view("custom/pkm-obsidian")`, then call
     `mcp_mcp-proxy_call_tool` → `obsidian_mcp_tools_search_vault_smart`.
     NEVER use grep, find, or search_files for semantic intent.
   - Literal/structural vault ops (read a specific known note, list a
     folder): use the `obsidian` skill with file tools on
     `/Volumes/DAL/Zettelkasten/LLMeon`.
   - Live Jira/Confluence/Todoist/Obsidian MCP APIs (create notes via
     API, fetch Jira issues, write to Obsidian REST): use `mcp_mcp-proxy_*`
     per `skill_view("custom/mcp-integration")` — never raw HTTP to port 8000.

Use patch (not full file rewrite). Verify the surrounding context matches
before applying.

### Task 3 — Patch pkm.yaml profile to load pkm-obsidian at session start

File: ~/.local/share/chezmoi/private_dot_hermes/profiles/pkm.yaml

Add a `skills` block that loads the skill automatically:

skills:
  preload:
    - custom/pkm-obsidian
    - custom/mcp-integration

And add to constraints:
  - Never use grep or find for semantic vault search — use obsidian_mcp_tools_search_vault_smart

## ACCEPTANCE CRITERIA

The task is complete when ALL of the following are true:

1. `~/.local/share/chezmoi/dot_hermes_custom_skills/custom/pkm-obsidian/SKILL.md`
   exists and contains the tool priority table, MCP discovery pattern, vault
   paths, and the grep/find hard rule.

2. SOUL.md §1.3 Triage Step 4 contains the three-way split
   (semantic PKM / literal file ops / live MCP APIs) with the explicit
   "NEVER use grep/find for semantic intent" instruction.

3. `pkm.yaml` contains `skills.preload` with both `custom/pkm-obsidian` and
   `custom/mcp-integration`, and the grep/find constraint.

4. `chezmoi apply --force` runs without errors.

5. `hermes doctor` shows no new issues introduced (1 pre-existing API keys
   issue is acceptable).

6. `hermes mcp list` still shows mcp-proxy, pieces, and graphify connected.

## DO NOT

- Edit files directly in ~/.hermes/ — that path is ephemeral (symlinks from
  chezmoi apply). Always write to ~/.local/share/chezmoi/ first.
- Change the primary model, provider, or API keys.
- Disable or rename any existing MCP servers.
- Rewrite SOUL.md in full — patch only Step 4.
- Touch auth.json, auth/, or any credential files.

## CHEZMOI WRITE PATHS

| Artefact | Write to |
|---|---|
| New pkm-obsidian skill | ~/.local/share/chezmoi/dot_hermes_custom_skills/custom/pkm-obsidian/SKILL.md |
| SOUL.md patch | ~/.local/share/chezmoi/private_dot_hermes/SOUL.md |
| pkm.yaml patch | ~/.local/share/chezmoi/private_dot_hermes/profiles/pkm.yaml |

After all writes: `chezmoi diff` to verify, then `chezmoi apply --force`.

British English throughout. Concise inline commentary only — no prose padding.
```

---

## Notes on the Prompt

Why it's structured this way—grounded from your actual chezmoi repo:

- The skill write path (`dot_hermes_custom_skills/custom/`) matches the `skills.external_dirs: [~/.hermes_custom_skills]` entry confirmed in `private_config.yaml` line 320. New custom skills go here, not under `private_dot_hermes/skills/`.
- The SOUL.md line 61 patch target is exact—confirmed from the filesystem text search result showing the current Step 4 text.
- The `pkm.yaml` preload block addresses the gap identified: the profile exists (confirmed at `private_dot_hermes/profiles/pkm.yaml`) but has no skill preloading and no explicit prohibition on grep.
- The acceptance criteria mirror the pattern from your prior successful `/goal` prompts (the startup fix prompt from 23 May showed exactly this structure: numbered AC list + DO NOT block + chezmoi path table).

---

## Asset 4 (Pieces: 72170dbe-1941-4e3a-97c2-5035a5be40e2)

The user wants me to create a ready-to-paste `/goal` prompt for Hermes that will:

1. Fix the PKM tooling issue (using grep/find instead of MCP semantic search / obsidian CLI)
2. Create/update the necessary skills
3. Do all config changes via the chezmoi repo (not directly to ~/.hermes/)

I need to produce a complete, self-contained `/goal` prompt that Hermes can execute. Let me use everything I've gathered from the memory search and filesystem reads to craft this.

Key facts I have:

- Hermes config is managed via chezmoi source at `~/.local/share/chezmoi/private_dot_hermes/`
- Custom skills go to `~/.local/share/chezmoi/dot_hermes_custom_skills/custom/<name>/SKILL.md` (per SOUL.md §7.2)
- Skills are loaded at runtime from `~/.hermes_custom_skills/` via `skills.external_dirs`
- The live path `~/.hermes/` is symlinks from chezmoi apply
- The obsidian skill is at `~/.hermes/skills/note-taking/obsidian/SKILL.md` - filesystem-first
- MCP integration lives at `~/.hermes/skills/mcp/mcp-integration/` with the main SOUL.md config at `~/.hermes/SOUL.md` (sourced from chezmoi)
- The vault is at `/Volumes/DAL/Zettelkasten/LLMeon`, the mcp-proxy runs on `http://127.0.0.1:8000/mcp/`, and obsidian-mcp-tools needs Obsidian listening on port 27124
- The Obsidian CLI provides commands like `obsidian create`, `obsidian search`, and `obsidian append`

The core issue is that Hermes relies on grep and find for vault searches instead of leveraging the semantic search capabilities through `obsidian_mcp_tools_search_vault_smart`, and SOUL.md's triage Step 4 incorrectly routes vault queries to the file-based obsidian skill rather than a proper PKM integration. I need to create a new `pkm-obsidian` skill in the chezmoi source, update SOUL.md's Step 4 routing to use it, then apply the changes with chezmoi.

Now I'm mapping out the exact file paths and crafting a precise `/goal` prompt for Hermes that includes what to read first, the specific changes needed, and verification steps before running `chezmoi apply`. I'm drafting the `/goal` prompt now, making sure it's self-contained so Hermes can execute it without needing clarification, with explicit file paths and a patch-based approach rather than overwriting files, plus verification steps to confirm everything worked.

---

## Asset 5 (Pieces: e176d7f3-538e-4058-8fac-d4a55d81b1c3)

Exploring search paths

I think I might need to verify if the root path exists first, or perhaps the repository isn't there at all. Looking for a README is an option, but the user is specifically after the chezmoi repo. It might be somewhere else in the sandbox, so I need to discover the right path. Although the current tool root is sandboxed, I should still check the broader filesystem, especially hidden directories under /Users/leon.ormes, possibly even in Documents. I'll focus on exact returned paths for my search.Considering search methods

I'm thinking about doing a parallel search for both paths and content, maybe even on broader roots. Since the review asks for this in one go, using `multi_tool_use.parallel` with both `filesystem_search_paths` and `filesystem_search_text` could work. However, it seems that the tool only allows developer tools, which is fine. I could search for "chezmoi" in /Users/leon.ormes, but the sandbox limits me. I might set roots to ["/Users/leon.ormes"] with include set to false.

---

## Asset 6 (Pieces: 712fd930-fff9-4e75-8c73-ff37afc5888e)

Both `pkm.yaml` was fully read in the last turn (`is_last: true`, 1546 bytes) and SOUL.md line 61 is already verbatim in the `filesystem_search_text` results. Reading both to confirm:
