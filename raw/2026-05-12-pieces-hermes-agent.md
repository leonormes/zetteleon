---
created: 2026-05-12T22:06:30+00:00
modified: 2026-05-26T11:43:45+00:00
pieces_ids: ["196c6cce-bc64-46f8-b66c-ea5cd31dbc8e"]
source: pieces-ltm
tags: [pieces, raw]
title: 2026-05-12-pieces-hermes-agent
---

## Pieces LTM Ingest—Hermes Agent

Ingestion window: Last 24 hours (4-hour window returned 0 new assets; extended per skill instructions)

Summary: 1 assets captured for Hermes Agent.

---

### Asset 1 (Pieces: 196c6cce-bc64-46f8-b66c-ea5cd31dbc8e)

Captured: 2026-05-12T08:39:51.322345Z

No new findings. Both drained pages return `recommendation: 'fetchMore'` on their respective chains, but the content is entirely duplicative of what was already captured in the synthesis—the same kube-state-metrics collision warnings, the same `fitfile-image-pull-secret` event counts, and the same Alloy job-label fix details (MR!758, `feature/FTFL-638-add-labels-for-logs`, `GRAFANA_ALLOY_FIX_PLAN.md`). One genuinely new detail surfaced from the second chain:

Additional artefact confirmed: A structured `GRAFANA_ALLOY_FIX_PLAN.md` was generated by the Hermes Agent on 6 May 2026, tracing to Jira tickets FTFL-638, FTFL-511/512, FTFL-599, and FTFL-596. This document was authored at `/Untitled (Workspace) — GRAFANA_ALLOY_FIX_PLAN.md` and includes the full current state, phase plan (verify → edit → sync → validate → stage promotion to production), and rollback criteria for the job-label fix. It was untracked at the time of capture—meaning it had not yet been committed, confirming MR!758 (`FTFL-638 Fix Alloy job label to use namespace/container format`) was its delivery vehicle.

Everything else in these drain pages—the Loki stream measurements (42 streams, 37 missing namespace), the endpoint fix at `ffnodes/fitfile/testing/values.yaml:273`, the `labelsToKeep` regression at `charts/ffnode/values.yaml:536`, the image-pull-secret count of 171-175 warnings—is fully accounted for in the prior synthesis. The deliverable is complete.

---

### FTFL-638—Missing Grafana Monitoring in Testing Cluster: Summary

Jira: [FTFL-638](https://fitfile.atlassian.net/browse/FTFL-638) | Type: Bug | Priority: Highest | Reporter: Ollie Rushton

---

#### Background

The testing cluster (`fitfile-cloud-testing-aks-cluster`, Grafana Cloud stack: `fitfiletest`) was migrated to the Grafana Alloy Helm chart (`grafana-k8s-monitoring v3.7.5`). After deployment, two categories of visibility were lost:

1. Loki logs—pods could be found by container name but not by pod name
2. Prometheus metrics—the Observability → Kubernetes pages showed no testing cluster nodes, pods, or components

---

#### Root Causes Identified

##### Root Cause 1—Wrong Prometheus Push Endpoint (fixed)

- File: `ffnodes/fitfile/testing/values.yaml:273`
- What broke: `alloy-metrics` was pushing to `/api/v1/write`. The Grafana Cloud Prometheus backend silently rejects that path; it requires `/api/prom/push`.
- Effect: All Prometheus metrics from the testing cluster were being silently dropped. Zero series flowing.

##### Root Cause 2—`labelsToKeep` Regression in Base Chart (fixed, then iterated)

- File: `charts/ffnode/values.yaml:536`
- What broke: A cardinality-reduction attempt set `labelsToKeep: ["pod"]` only. The live Alloy config showed `stage.label_keep { values = ["_tenant_id_", "pod"] }`, stripping `namespace` and `container` from all pod log streams before shipping to Loki.
- Effect measured live: 42 total log streams in the last 30 min; 37/42 pod log streams lost `namespace` and `container` as queryable labels. Any query like `{namespace="fitfile"}` returned nothing—logs could only be found by exact pod name (fragile, breaks on redeploy).

##### Root Cause 3—Schema Mismatch: `podLogs` Vs `logs.pod_logs` (fixed)

- File: `ffnodes/fitfile/testing/values.yaml`
- What broke: The v3.x chart uses the key `logs.pod_logs.enabled`, not `podLogs.enabled`. The wrong key name meant the pod logs pipeline may not have been activating at all after the chart upgrade.

##### Root Cause 4—Broken `job` Label Relabel Rule in Chart V3.7.5 (fixed)

- Scope: Specific to `grafana-k8s-monitoring v3.7.5`
- What broke: The chart generates a broken relabel rule for the `job` label on pod logs—it drops the container name, producing `job="namespace"` instead of `job="namespace/container"`. This breaks log filtering and analysis by job.
- Fix: Added an `extraRelabelingRules` block to `values.yaml` with `source_labels = ["namespace", "container"]` and `target_label = "job"` to override the broken chart rule.

---

#### Changes Made—Commits & MRs

| MR | Title | Branch | Status |
|---|---|---|---|
| [!751](https://gitlab.com/fitfile/deployment/-/merge_requests/751) | FTFL-638 Introduce graphify knowledge graph and agents integration | `feature/FTFL-638-missing-grafana-monitoring-in-testing-cluster` | Merged (`6a641342`) |
| [!752](https://gitlab.com/fitfile/deployment/-/merge_requests/752) | FTFL-638 Add `namespace` and `container` labels to Grafana monitoring | `feature/FTFL-638-add-missing-labels` | Merged |
| [!753](https://gitlab.com/fitfile/deployment/-/merge_requests/753) | FTFL-638 Fix Grafana pod log configuration path | `feature/FTFL-638-fix-log-enabled` | Merged (`09360081`) |
| [!758](https://gitlab.com/fitfile/deployment/-/merge_requests/758) | FTFL-638 Fix Alloy job label to use namespace/container format | `feature/FTFL-638-add-labels-for-logs` | Merged |

Primary root-fix commit: `33a069d6`—_"FTFL-638 Update Grafana configurations for log labels and metric ingestion"_—Thu 30 Apr 2026, authored by Leon Ormes (`leon.ormes@fitfile.com`).

Key file changes across the branch:

- `ffnodes/fitfile/testing/values.yaml`—Prometheus endpoint fix (`/api/v1/write` → `/api/prom/push`), `labelsToKeep`, schema rename (`podLogs` → `logs.pod_logs`), `excludeNamespaces: [dataprotection-microsoft]`
- `charts/ffnode/values.yaml`—Base chart `labelsToKeep` restored to `["pod", "namespace", "container"]`
- `ffnodes/fitfile/testing/values.yaml` (MR!758)—`extraRelabelingRules` block added to fix chart v3.7.5's broken `job` label

A detailed fix plan was generated by the Hermes Agent on 6 May 2026 (`GRAFANA_ALLOY_FIX_PLAN.md`), covering current state, phase plan (verify → edit → sync → validate → staged promotion to production clusters), and rollback criteria for the job-label fix.

---

#### Post-Fix Verification (5 May 2026)

Confirmed by running `gcx logs query` and `gcx metrics query` against `fitfiletest`:

- Logs: Flowing from testing cluster to Loki. Stream labels trimmed to `cluster`, `pod`, `k8s_cluster_name`, `detected_level`, `service_name`.
- Metrics: 318 active series flowing from `cluster="testing"` in Prometheus.
- Alloy pods: 3× `alloy-logs` DaemonSet pods, 1× `alloy-metrics` StatefulSet, 1× `alloy-singleton` Deployment—all Running, no errors in logs.
- gcx OAuth note: The OAuth token for `fitfiletest` expired 2026-05-01; re-authentication (`gcx login --context fitfiletest`) was required for advanced Loki dashboard queries (cloud metrics token remained valid).

---

#### Outstanding / Open Items (not yet fixed)

##### Issue 1—`kube-state-metrics` Timestamp Collision (ongoing Data loss)

- Symptom: `alloy-metrics-0` logs this every 60 seconds:

  ```
  level=warn msg="Error on ingesting samples with different value but same timestamp"
  component_id=prometheus.scrape.kube_state_metrics num_dropped=1
  ```

- Root cause hypothesis: The discovery config uses `role = "endpoints"` for kube-state-metrics. If the service exposes multiple ports (readiness/metrics split), Alloy scrapes the same target twice in the same 60s window and gets conflicting values—one sample is dropped.
- Recommended fix: Inspect `kubectl get endpoints grafana-k8s-monitoring-kube-state-metrics -n monitoring -o yaml` for multiple ports; switch discovery to `role = "service"` or constrain the port selector.
- Status: Identified, not yet addressed in any PR.

##### Issue 2—`fitfile-image-pull-secret` Missing Cluster-wide (pre-existing)

- Symptom: 171–175+ `FailedToRetrieveImagePullSecret` warnings across `aks-system` and other namespaces (visible in Grafana Loki, `{namespace="thehyve", cluster="testing"}`).
- Impact: Pods are currently running from node cache. Any pod rescheduled to a node without a cached image will fail to start.
- Note: This is pre-existing and unrelated to FTFL-638 itself, but was discovered during the investigation. Needs a separate ticket.

##### Issue 3—`job` Label Fix (MR!758) not yet Promoted to Production Clusters

- The `extraRelabelingRules` fix for the broken `job` label is in the testing cluster. The fix plan (`GRAFANA_ALLOY_FIX_PLAN.md`) identifies staged promotion to `mcnft-prod-1` and `cuh-prod-1` as the next steps once validated in testing.

---
