---
aliases: []
confidence: 
created: Invalid date
epistemic: 
last_reviewed: 
modified: 2025-12-15T10:37:34Z
purpose: 
review_interval: 
see_also: []
source_of_truth: []
status: raw
tags: [state/thinking]
title: HEAD - 2025-12-15 1036
type: head
uid: 
updated: 
---

Why are there so many changes to hie via terraform cloud!!!!

It is annoying and frustrating.

I get this from gemini 3

```md
Based on the logs provided, here is the architectural assessment of your Terraform plan.

### **Executive Summary**

Is it safe? Likely, yes.

The plan indicates 0 resources to destroy, 0 to add, and 17 to change. This is an "update-in-place" operation. However, caution is required regarding the EKS Node Groups, as "updates" to these resources can trigger instance rotation (rolling updates), which might temporarily impact workload availability depending on your Pod Disruption Budgets.

### **What is happening?**

The logs explicitly show **"Drift detected (update)"** across multiple disparate resources. This means the actual infrastructure in AWS currently differs from what is recorded in your Terraform state file. Terraform is planning to update your resources to reconcile these differences.

The widespread nature of the changes (spanning IAM, EKS, Load Balancers, and VPC Endpoints) suggests a systemic trigger rather than a specific configuration change. The two most likely causes are:

1. **Tagging Discrepancies:** A change in `default_tags` at the AWS Provider level, or manual tags applied via the AWS Console that Terraform is now trying to remove/align.
    
2. **Provider/Platform Defaults:** AWS or the Terraform provider may have changed default values (e.g., S3 default encryption or EKS versions) which Terraform is now enforcing.
    

### **Detailed Change Analysis**

#### **1. EKS & Compute (Medium Risk)**

- **Resources:** `module.eks.aws_eks_node_group`, `module.eks.aws_launch_template`, `module.eks.aws_eks_cluster`.
    
- **Observation:** The logs show drift on the Node Groups and a plan to update the Launch Templates3.
    
- **Implication:** If Terraform updates a Launch Template (e.g., refreshing the AMI ID or User Data), the associated Node Group update usually triggers a rolling replacement of your worker nodes. **Verify the diff:** Ensure this is just a metadata/tag update. If it involves AMIs or User Data, expect node recycling.
    

#### **2. Networking & Load Balancing (Low Risk)**

- **Resources:** `aws_lb` (Relay Public/Preprod), `aws_lb_target_group`, `module.gateway.aws_nat_gateway`.
    
- **Observation:** Both Load Balancers and Target Groups are marked for update.
    
- **Implication:** These are likely attribute reconciliations (e.g., `drop_invalid_header_fields` or tags). These updates rarely cause traffic interruption unless listeners are being modified (which does not appear to be the case here).
    

#### **3. IAM & Security (Low Risk)**

- **Resources:** `aws_iam_role` (Node Group, Cluster), `aws_iam_policy`, `aws_iam_openid_connect_provider`.
    
- **Observation:** Multiple roles and policies are updating5.
    
- **Implication:** IAM updates usually involve tag synchronization or JSON policy normalization (e.g., reordering whitespace). These are generally non-disruptive.
    

#### **4. Storage & Endpoints (Low Risk)**

- **Resources:** `aws_vpc_endpoint` (S3, SSM), `aws_s3_bucket_server_side_encryption_configuration`.
    
- **Observation:** Drift detected on VPC endpoints and S3 encryption.
    
- **Implication:** AWS recently enforced default encryption on all S3 buckets (SSE-S3). If your Terraform code specifies explicit encryption or was written prior to this change, Terraform is likely just updating the state to match AWS's enforced reality.
    

### **Next Steps**

1. **Inspect the `tags`:** Run `terraform plan` locally or check the expanded diff in your CI. If 90% of the lines are `~ tags = { ... }`, the apply is completely safe.
    
2. **Check EKS Launch Templates:** Look specifically at `module.eks.aws_launch_template`. If you see changes to `image_id` or `user_data`, ensure you are ready for a node rotation.
    
3. **Approve:** If the above checks pass, the plan is safe to apply.
```

It is difficult to debug without the argocd cli or the frontend for it. I can't tell if the app is working or not. Everyone is busy. Not sure how to proceed. 
## HEAD 2025-12-15 10:36

---

### 🛑 The Compile (Exit Strategy)
*When the thinking stops, extract the value and archive this note.*

- [ ] **The Next Test:** (What is the binary Pass/Fail action?)
- [ ] **The Update:** (Did I update the relevant SoT?) `[[Link SoT Here]]`

---

### 🚀 The Tri-State Output (Router)
*Where does this thinking go next? Pick one path.*

> **Path A: Kinetic (Action)**
> *I figured it out. I just need to do it.*
> - [ ] **Task:** (Write the binary Next Action here) `-> Send to Todoist`

> **Path B: Static (Storage)**
> *I learned a fact / defined a spec. No action needed.*
> - [ ] **SoT Update:** (What did I learn?) `-> Merge into [[SoT - Topic]]`

> **Path C: Dynamic (Still Thinking)**
> *I am pausing this thread. Serialize state to resume later.*
>
> 1. [ ] **The Save State:** (Write 1 sentence: "I stopped at X, the very next step is Y")
>    - *Constraint: If you don't write this, you fail the 60-Second Restore Test.*
> 2. [ ] **The Pointer:** (Create Todoist Task with Link) `-> Schedule for [Date]`
> 3. [ ] **TTL Check:** (Is this the 3rd resume? If yes, refactor to Project).
