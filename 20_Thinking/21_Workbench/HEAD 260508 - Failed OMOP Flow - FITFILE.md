---
captured: "2026-05-11T09:07:13+01:00 2026-05-11T09:07:13+01:00"
created: 2026-05-11T08:07:24+00:00
modified: 2026-05-11T08:11:57+00:00
source: "https://fitfile.atlassian.net/wiki/spaces/FITFILE/pages/2799140865/260508+-+Failed+OMOP+Flow"
status: "processing"
tags: ["input"]
title: HEAD 260508 - Failed OMOP Flow - FITFILE
type: "head"
---

## Raw Output / Content

## 260508 - Failed OMOP Flow

## Postmortem Summary

<table><colgroup><col> <col></colgroup><tbody><tr><th rowspan="1" colspan="1"><div><p><strong>Postmortem owner</strong></p><figure></figure></div></th><th rowspan="1" colspan="1"><div><figure></figure></div></th></tr></tbody></table>

<table><colgroup><col> <col></colgroup><tbody><tr><th rowspan="1" colspan="1"><div><p><strong>Postmortem owner</strong></p><figure></figure></div></th><th rowspan="1" colspan="1"><div><figure></figure></div></th></tr><tr><td rowspan="1" colspan="1"><p><strong>Severity</strong></p></td><td rowspan="1" colspan="1"><p>High</p></td></tr><tr><td rowspan="1" colspan="1"><p><strong>Affected services</strong></p></td><td rowspan="1" colspan="1"><ul><li><p>FITConnect, workflows-api, data pipeline</p></li></ul></td></tr></tbody></table>

## Executive Summary

SDE data extracts failed twice for the same query. Nodepool scale-ups and scale-downs during query workload caused services to become temporarily unavailable.

## Postmortem Report

<table><colgroup><col> <col></colgroup><tbody><tr><th rowspan="1" colspan="1"><div><p><strong>Instructions</strong></p><figure></figure></div></th><th rowspan="1" colspan="1"><div><p><strong>Report</strong></p><figure></figure></div></th></tr></tbody></table>

<table><colgroup><col> <col></colgroup><tbody><tr><th rowspan="1" colspan="1"><div><p><strong>Instructions</strong></p><figure></figure></div></th><th rowspan="1" colspan="1"><div><p><strong>Report</strong></p><figure></figure></div></th></tr><tr><td rowspan="1" colspan="1"><h3>Detection</h3></td><td rowspan="1" colspan="1"><p>SDE contacted us through the SLA-EoE emails (which developers do not seem to be apart of)…</p></td></tr><tr><td rowspan="1" colspan="1"><h3>Impact</h3></td><td rowspan="1" colspan="1"><p>2 repeated workflow failures for the same workflow extract within 4 hours of eachother.</p><p>The customer might be unable to service their data access request within the desired timeframe.</p><p>Loss of customer confidence in the reliability of our system.</p></td></tr><tr><td rowspan="1" colspan="1"><h3>Investigation</h3></td><td rowspan="1" colspan="1"><p>The <code>workflows-api</code> pod was <strong>starting up at 09:48:02</strong>—meaning it had just restarted at ~09:47, seconds before the error was logged.</p><p><code>10.2.0.130:80</code> <strong>was a</strong> <code>workflows-api</code> <strong>pod</strong> that terminated. The workflow task held a reference to that pod's IP directly and attempted to call it just as the pod went down.</p><p><strong>Summary:</strong></p><ul><li><p><code>workflows-api</code> pod at <code>10.2.0.130</code> restarted at ~09:47</p></li><li><p>Workflow task <code>sb24z</code> tried to reach it by pod IP (not service DNS) and got <code>ECONNREFUSED</code></p></li><li><p><code>ffcloud-service</code> picked up the failure via MongoDB change stream and logged <code>OperationFailed</code></p></li><li><p>Both services recovered within ~2 minutes; the workflow eventually succeeded at 14:35</p></li></ul><hr><p><strong>Root cause identified: AKS node</strong> <code>aks-system-23892849-vmss000019</code> <strong>was drained at 09:47:26.</strong></p><p>At exactly 09:47:26 UTC, Kubernetes sent SIGTERM to <strong>every pod running on that node</strong> simultaneously—a classic node drain event (maintenance, scale-down, or AKS upgrade). The evidence:</p><ul><li><p><code>coredns</code>, <code>calico-node</code>, <code>kube-proxy</code>, <code>konnectivity-agent</code>, <code>azure-cns</code>, <code>vpa-recommender</code>, <code>cuh-prod-1-fitconnect-ftc</code>, <code>cuh-prod-1-minio</code>, <code>spicedb-postgresql</code>, and ~15 other pods all received <code>Killing</code> events from kubelet on <code>aks-system-23892849-vmss000019</code> at the exact same second</p></li><li><p><code>workflows-api</code> was running on that node → received SIGTERM → graceful Uvicorn shutdown at 09:47:26 → restarted on a new node/pod by 09:48:02</p></li></ul><p>A separate, unrelated event followed: the <code>ffcloud-service</code> pod failed its <strong>liveness probe</strong> at 09:48:29 (on a different node, <code>vmss00000b</code>) and was also restarted.</p><p><strong>Full chain:</strong></p><ol><li><p><code>vmss000019</code> node drained at 09:47:26 → <code>workflows-api</code> pod at <code>10.2.0.130</code> terminated</p></li><li><p>Workflow task <code>sb24z</code> tried to call <code>10.2.0.130:80</code> (stale pod IP) → <code>ECONNREFUSED</code></p></li><li><p><code>ffcloud-service</code> logged <code>OperationFailed</code>, then also restarted due to a liveness probe failure</p></li><li><p>Both services recovered within ~2 minutes; workflow retried and succeeded at 14:35</p></li></ol><hr><img src="blob:https://fitfile.atlassian.net/95ad55b4-879f-4c6b-b3f5-ad4da9e58fe4#media-blob-url=true&id=fa80b53c-1615-4901-b3ed-92ed5b7928bf&collection=contentId-2799140865&contextId=2799140865&mimeType=image%2Fpng&name=image-20260508-160645.png&size=75257&width=717&height=336&alt=image-20260508-160645.png&clientId=79d9a90a-d0b1-4ba1-9c4d-7fda8295d8c2"><hr><p>The scale-up was triggered by the <code>a0025-extract-1-workflow-wjjtp</code> workflow itself (the same workflow that had the first failure).</p><p>At <strong>04:38:14 UTC</strong>, 8 <code>ohdsi-query-combiner</code> pods from that workflow were all pending simultaneously and couldn't be scheduled. This triggered the <strong>workflows nodepool</strong> (<code>aks-workflows-38355244-vmss</code>) to scale from <strong>1→4 nodes</strong>.</p><p>A separate, secondary scale-up followed at <strong>04:44:27 UTC</strong>: the new workflow nodes needed a <code>calico-typha</code> pod for networking, which couldn't fit on the existing 2 system nodes—so it triggered the <strong>system nodepool</strong> (<code>aks-system-23892849-vmss</code>) to scale from <strong>2→3 nodes</strong>. That's the system node we see appear at 04:52.</p><p>So the chain was:</p><ul><li><p><code>wjjtp</code> workflow burst of <code>ohdsi-query-combiner</code> pods → workflows nodepool scaled 1→4</p></li><li><p>Calico networking component needed for new nodes → system nodepool scaled 2→3</p></li><li><p>Once the burst completed, autoscaler removed the idle nodes → workflows scaled back down, then system scaled back 3→2 (at 05:37, draining <code>vmss000018</code> and causing the first workflow failure)</p></li></ul><p>The system node scale-up was entirely a <strong>side-effect</strong> of the workflow workload, not a direct application demand on the system pool.</p></td></tr><tr><td rowspan="1" colspan="1"><h3>Recovery</h3></td><td rowspan="1" colspan="1"><p>The cluster self healed, however, the workflow is marked as failed.</p></td></tr><tr><td rowspan="1" colspan="1"><h3>Five whys root cause identification</h3></td><td rowspan="1" colspan="1"><p>Why did the workflow fail with ECONNRESET</p><ul><li><p>Because the Workflows API pod was shutdown and the polling fitconnect service treats ECONNRESET as a workflow failure as opposed to a transient error (which shouldn’t lead to failure)</p></li><li><p>Why? - Because the system nodepool had a scaledown which evicted the pods</p></li><li><p>Why? - Because more calico-typha replicas were deployed to serve the 4 new workflows nodes which came up, and there wasn’t room on the system node pool for more calico pods</p></li><li><p>Why? - Because we deploy calico components which might not be needed, and calico-typha has a replica set autoscaler, and the pods do not have tolerations to be placed on the workflows nodepool</p></li><li><p>Why? - because we do not pay attention enough to correctly configuring our kubernetes clusters, because we do not have experts in doing so…</p></li></ul></td></tr><tr><td rowspan="1" colspan="1"><h3>Blameless root cause resolution</h3></td><td rowspan="1" colspan="1"><p>Multiple things to fix:</p><ul><li><p>Treat ECONNRESET in polling as a transient network error that does not lead to failure of polling. - <span><a href="https://fitfile.atlassian.net/browse/FTFL-653">FTFL-653: FITConnect Workflows Manager - ECONNRESET marked as failureReady for test</a></span></p></li><li><p>Research calico components as figure out whether we either need to - <span><a href="https://fitfile.atlassian.net/browse/FTFL-464">FTFL-464: Remove Calico Cloud Components from AKS/EKS ClustersBacklog</a></span>:</p><ul><li><p>Remove redundant components which we don’t use and are causing issues</p></li><li><p>Identify their behaviour and correctly configure them (perhaps with tolerations as a start) to not disrupt the core services.</p></li></ul></li><li><p>Add pod disruption budgets and figure out how to make replacement replicas come up before node’s are drained/evicted.</p><ul><li><p>This is wrong and too difficult after second thoughts (because involves mongodb replicas…). Instead, we should make the system nodepool fixed size - it should never grow or shrink, but always have enough capacity for the core FITFILE services - <span><a href="https://fitfile.atlassian.net/browse/FTFL-654">FTFL-654: Fix the size of our Production System NodePoolsBacklog</a></span></p></li><li><p>This does not block kubernetes version upgrades as the fixed fix only applies to the autoscaler, whereas k8s version upgrades create surge nodes - the same applies to node image upgrades.</p></li></ul></li><li><p>Hire specialists to help analyse and correctly configure our kube-system components and to help make our deployments more reliable.</p></li><li><p>Have resources/time to setup proper incident monitoring and cluster health so we can identify these issues before customers have to inform us of these issues.</p></li></ul></td></tr></tbody></table>

## Supporting Documents

Related content
